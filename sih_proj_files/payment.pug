block variables 
doctype html 
html 
    head 
        script (src = "/public/client.js")
        //link(rel="stylesheet", href="/style.css")app.use(express.static(path.join(__dirname, "public")));
        meta(charset="utf-8")
        link(rel="shortcut icon", href="/favicon.ico")
        meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
        meta(name="theme-color", content="#000000")
        title #{title} | Registration Portal 
        //script(src='/public/client.js')
        link(rel='stylesheet', href = "style.css")
        style.
            body {
                    color: #f1faee;
                    margin:0;
                    padding:0;
                    background: url(img.png) no-repeat center center fixed; 
                    -webkit-background-size: cover;
                    -moz-background-size: cover;
                    -o-background-size: cover;
                    background-size: cover;
                    position: absolute;
                 }
            .main-section {
                    display:flex;
                    flex-direction:column;
                    justify-content:center;
                    width:40vw;
                    margin-left:auto;
                    margin-right:auto;
                    background-color: #8695a3;
                 }
            form{   color:#1d3557;
                    display:flex;
                    flex-direction:column;
                    justify-content:center;
                    background-color:white;
                    padding:30px;

            }
            form label{
                    font-size:20px;
                    padding:5px 0px;
            }
            form input{
            }
            .head {
                    font-size:50px;
                    text-align:center;
                    padding:10;
                    margin:10px;
                    background: linear-gradient(to right,
                                                violet,
                                                indigo,
                                                blue,
                                                green,
                                                yellow,
                                                orange,
                                                red);
                               -webkit-background-clip: text;
                    color: transparent;
            }
            .sub-head{
                    font-size:30px;
                    text-align:center;
                    padding:10px;
                    margin:0;
            }
            button{
                font-size:24px;
                height:36px;
                color:#f1faee;
                background: linear-gradient(to right, #ff105f, #ffad06);
                width:248px;
                margin-left:auto;
                margin-right:auto;
                margin-top:20px;
                margin-bottom:20px;
                border:none;
                border-radius:20px;
                padding-block: 1rem;
            }
    body 
        .main-section 
            p.head  Payment Portal
            .about-us 
                p.sub-head Enter Your OTP To Complete Your Payment
            form(role='form' method='POST' action='/verify')
                label(for='Username') Username:
                input#Username(type='text', placeholder='Email/Mobile' name='Username')
                label(for='OTP') OTP:
                input#OTP(type='number', placeholder='check your app for OTP' name='OTP')
                button#submit-btn(type="submit", name="asdf") Submit